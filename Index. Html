
```html
<!DOCTYPE html>
<html>
<head>
  <title>Genre Lagu Editor - Herlandos</title>
  <style>
    body { font-family: Arial; text-align: center; padding: 30px; background: #f0f0f0; }
    .box { background: white; padding: 20px; border-radius: 10px; max-width: 400px; margin: auto; }
    select, input, button { margin: 10px; padding: 10px; width: 90%; }
  </style>
</head>
<body>
  <div class="box">
    <h2>Edit Genre Lagu 🎵</h2>
    <input type="file" id="fileInput" accept="audio/*" />
    <select id="genreSelect">
      <option value="rock">Rock</option>
      <option value="jazz">Jazz</option>
      <option value="edm">EDM</option>
      <option value="metal">Metal</option>
      <option value="pop">Pop</option>
    </select>
    <select id="vocalSelect">
      <option value="male">Vokal Pria</option>
      <option value="female">Vokal Wanita</option>
    </select>
    <button onclick="generateLagu()">Generate Lagu</button>

    <p id="statusText"></p>
[13/10 13.07] Chat_GPT: <audio id="audioPlayer" controls style="display:none;"></audio>
  </div>

  <script>
    function generateLagu() {
      const file = document.getElementById('fileInput').files[0];
      const genre = document.getElementById('genreSelect').value;
      const vocal = document.getElementById('vocalSelect').value;

      if (!file) {
        alert("Mohon upload lagu terlebih dahulu.");
        return;
      }

      document.getElementById('statusText').innerText = "Mengirim lagu ke server AI...";
      
      // Simulasi response
      setTimeout(() => {
        document.getElementById('statusText').innerText = "Genre berhasil diubah ke " + genre + " dengan vokal " + vocal;
        const audio = document.getElementById('audioPlayer');
        audio.src = "https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3"; // contoh file hasil
        audio.style.display = "block";
      }, 3000);
    }
  </script>
</body>
</html>
```
